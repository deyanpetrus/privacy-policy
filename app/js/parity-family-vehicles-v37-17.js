(()=>{
'use strict';
if(new URLSearchParams(location.search).get('workspace')!=='desktop')return;
const ac=x=>Array.isArray(x?.attachments)?x.attachments.length:0;
try{familyPage=function(){const members=collection('family_members'),tasks=collection('family_tasks'),shop=collection('family_shopping'),events=collection('family_events');return`${pageHead('Family','Personal family records plus optional RLS-protected Shared Space.',`<button class="quiet" data-family-group>Shared Space</button><button class="primary" data-add-family>＋ Add</button>`)}<div class="grid"><div class="card span-6"><h3>Members</h3><div class="list">${members.map(x=>`<div class="list-item"><div class="grow"><b>${esc(x.name)}</b><span>${esc(x.relation||'')}${x.birthday?' · '+fmtDate(x.birthday):''}</span></div>${ac(x)?attachmentsHtml(x.attachments):''}<button class="mini-btn" data-edit-family-member="${x.id}">Edit</button><button class="mini-btn" data-del-family-member="${x.id}">Delete</button></div>`).join('')||'<span class="card-sub">No members</span>'}</div></div><div class="card span-6"><h3>Tasks</h3><div class="list">${tasks.map(x=>`<div class="list-item"><input type="checkbox" data-toggle-task="${x.id}" ${x.done?'checked':''}><div class="grow"><b>${esc(x.title)}</b><span>${esc(x.assignedTo||'')}${x.dueDate?' · '+fmtDate(x.dueDate):''}</span></div>${ac(x)?attachmentsHtml(x.attachments):''}<button class="mini-btn" data-edit-family-task="${x.id}">Edit</button><button class="mini-btn" data-del-family-task="${x.id}">Delete</button></div>`).join('')||'<span class="card-sub">No family tasks</span>'}</div></div><div class="card span-6"><h3>Shopping</h3><div class="list">${shop.map(x=>`<div class="list-item"><input type="checkbox" data-toggle-shop="${x.id}" ${x.checked?'checked':''}><div class="grow"><b>${esc(x.name)}</b><span>${esc(x.qty||'')}</span></div><button class="mini-btn" data-edit-family-shop="${x.id}">Edit</button><button class="mini-btn" data-del-family-shop="${x.id}">Delete</button></div>`).join('')||'<span class="card-sub">List is empty</span>'}</div></div><div class="card span-6"><h3>Events</h3><div class="list">${events.map(x=>`<div class="list-item"><div class="grow"><b>${esc(x.title||'Event')}</b><span>${fmtDate(x.date||x.dueDate)} · ${esc(x.type||'event')}</span></div>${ac(x)?attachmentsHtml(x.attachments):''}<button class="mini-btn" data-edit-family-event="${x.id}">Edit</button><button class="mini-btn" data-del-family-event="${x.id}">Delete</button></div>`).join('')||'<span class="card-sub">No events</span>'}</div></div></div>`}}catch{}
try{familyAddChooser=function(){modal('Add to Family',`<div class="modal-body"><div class="quick-cards"><button class="quick-card" data-family-kind="member"><b>👤 Member</b><span>Person & contact</span></button><button class="quick-card" data-family-kind="task"><b>✓ Task</b><span>Assignment & due date</span></button><button class="quick-card" data-family-kind="shopping"><b>🛒 Shopping</b><span>Item & quantity</span></button><button class="quick-card" data-family-kind="event"><b>📅 Event</b><span>Date & reminder</span></button></div></div>`,root=>root.onclick=e=>{const k=e.target.closest('[data-family-kind]')?.dataset.familyKind;if(k==='member')familyMemberModal({});else if(k==='task')familyTaskModal({});else if(k==='shopping')familyShopModal({});else if(k==='event')familyEventModal({})})}}catch{}
try{familyMemberModal=function(x={}){modal(x.id?'Edit member':'Family member',`<form id="fmForm" class="modal-form"><input type="hidden" name="id" value="${esc(x.id||id('member'))}"><label>Name<input required name="name" value="${esc(x.name||'')}"></label><div class="form-grid"><label>Relation<input name="relation" value="${esc(x.relation||'')}"></label><label>Birthday<input type="date" name="birthday" value="${esc(day(x.birthday))}"></label></div><label>Phone<input name="phone" value="${esc(x.phone||'')}"></label><label>Notes<textarea name="notes">${esc(x.notes||'')}</textarea></label><label>Attachment<input type="file" name="file"></label>${attachmentsHtml(x.attachments||[])}<div class="form-actions"><button class="quiet" type="button" data-close>Cancel</button><button class="primary">Save</button></div></form>`,root=>{$('#fmForm',root).onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,v=formData(f);let atts=[...(x.attachments||[])];try{if(f.file.files[0])atts.push(await D.upload('family',v.id,f.file.files[0]));await D.upsert('family_members',{...x,...v,attachments:atts,updatedAt:iso()});await reload('family_members');closeModal();toast('Family member synced');render()}catch(err){toast(err.message,true)}}})}}catch{}
try{familyTaskModal=function(x={}){modal(x.id?'Edit task':'Family task',`<form id="ftForm" class="modal-form"><input type="hidden" name="id" value="${esc(x.id||id('task'))}"><label>Task<input required name="title" value="${esc(x.title||'')}"></label><div class="form-grid"><label>Assigned to<input name="assignedTo" value="${esc(x.assignedTo||'')}"></label><label>Due date<input type="date" name="dueDate" value="${esc(day(x.dueDate))}"></label></div><label>Priority<select name="priority">${['low','medium','high'].map(v=>`<option ${x.priority===v?'selected':''}>${v}</option>`).join('')}</select></label><label>Attachment<input type="file" name="file"></label>${attachmentsHtml(x.attachments||[])}<div class="form-actions"><button class="quiet" type="button" data-close>Cancel</button><button class="primary">Save</button></div></form>`,root=>{$('#ftForm',root).onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,v=formData(f);let atts=[...(x.attachments||[])];try{if(f.file.files[0])atts.push(await D.upload('family',v.id,f.file.files[0]));await D.upsert('family_tasks',{done:false,...x,...v,attachments:atts,updatedAt:iso()});await reload('family_tasks');closeModal();toast('Family task synced');render()}catch(err){toast(err.message,true)}}})}}catch{}
function familyEventModal(x={}){modal(x.id?'Edit event':'Family event',`<form id="feForm" class="modal-form"><input type="hidden" name="id" value="${esc(x.id||id('event'))}"><label>Title<input required name="title" value="${esc(x.title||'')}"></label><div class="form-grid"><label>Type<select name="type">${['event','birthday','appointment','other'].map(v=>`<option ${x.type===v?'selected':''}>${v}</option>`).join('')}</select></label><label>Date<input type="date" name="date" value="${esc(day(x.date||x.dueDate)||day(iso()))}"></label></div><label>Notes<textarea name="notes">${esc(x.notes||'')}</textarea></label><label>Attachment<input type="file" name="file"></label>${attachmentsHtml(x.attachments||[])}<div class="form-actions"><button class="quiet" type="button" data-close>Cancel</button><button class="primary">Save</button></div></form>`,root=>{$('#feForm',root).onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,v=formData(f);let atts=[...(x.attachments||[])];try{if(f.file.files[0])atts.push(await D.upload('family',v.id,f.file.files[0]));await D.upsert('family_events',{...x,...v,attachments:atts,updatedAt:iso()});await reload('family_events');closeModal();toast('Family event synced');render()}catch(err){toast(err.message,true)}}})}
try{familyGroupModal=async function(){let groups=[];try{groups=await D.familyGroups()}catch{}modal('Family Shared Space',`<div class="modal-body"><div class="settings-card"><h3>${groups.length?'Connected shared space':'Create or join a shared space'}</h3><p class="card-sub">Family sharing uses Supabase RLS. Join using the invitation code created by the family group.</p>${groups.map(g=>`<div class="list-item"><div class="grow"><b>${esc(g.name)}</b><span>Invitation code: <strong>${esc(g.share_code||'')}</strong></span></div></div>`).join('')||'<p class="card-sub">You are not currently in a shared group.</p>'}</div><div class="form-grid"><form id="createGroup" class="settings-card modal-form"><h3>Create</h3><label>Group name<input name="name" placeholder="My family"></label><button class="primary">Create secure group</button></form><form id="joinGroup" class="settings-card modal-form"><h3>Join</h3><label>Invitation code<input name="code" required maxlength="32" placeholder="Enter code"></label><button class="quiet">Join shared space</button></form></div></div>`,root=>{$('#createGroup',root).onsubmit=async e=>{e.preventDefault();try{const g=await D.createFamily(formData(e.currentTarget).name);toast(`Group created · ${g.share_code||''}`);closeModal()}catch(err){toast(err.message,true)}};$('#joinGroup',root).onsubmit=async e=>{e.preventDefault();try{await D.joinFamily(formData(e.currentTarget).code.trim());toast('Joined family shared space');closeModal()}catch(err){toast(err.message,true)}}})}}catch{}
try{vehiclesPage=function(){const vs=collection('vehicles');return`${pageHead('Vehicles & VIN','Vehicle records, service, fuel, files and NHTSA VIN decode.',`<button class="quiet" data-vin>VIN lookup</button><button class="primary" data-add-vehicle>＋ Vehicle</button>`)}<div class="grid">${vs.map(v=>`<div class="card span-6"><h3>${esc(v.name||v.licensePlate||'Vehicle')}</h3><span class="card-sub">${esc([v.brand,v.model,v.year].filter(Boolean).join(' '))}</span><div class="stat-row"><div class="stat"><b>${esc(v.currentKm||'—')}</b><span>KM</span></div><div class="stat"><b>${fmtDate(v.registrationExpires)}</b><span>Registration</span></div><div class="stat"><b>${(v.services||[]).length}</b><span>Services</span></div><div class="stat"><b>${(v.fuelHistory||[]).length}</b><span>Fuel logs</span></div></div>${ac(v)?`<div class="parity-card-files">${attachmentsHtml(v.attachments)}</div>`:''}<div class="list parity-vehicle-history">${(v.services||[]).slice(-2).reverse().map(s=>`<div class="list-item"><div class="grow"><b>Service · ${esc(s.type)}</b><span>${fmtDate(s.date)} · ${esc(s.km||0)} km · ${fmtMoney(s.cost||0,state.profile?.currency||'EUR')}</span></div>${ac(s)?attachmentsHtml(s.attachments):''}</div>`).join('')}${(v.fuelHistory||[]).slice(-1).map(f=>`<div class="list-item"><div class="grow"><b>Fuel · ${esc(f.station||'station')}</b><span>${fmtDate(f.date)} · ${esc(f.liters||0)} L · ${fmtMoney(f.total||0,state.profile?.currency||'EUR')}</span></div></div>`).join('')}</div><div class="page-actions" style="margin-top:14px"><button class="mini-btn" data-edit-vehicle="${v.id}">Edit</button><button class="mini-btn" data-service-vehicle="${v.id}">＋ Service</button><button class="mini-btn" data-fuel-vehicle="${v.id}">＋ Fuel</button><button class="mini-btn" data-del-vehicle="${v.id}">Delete</button></div></div>`).join('')||`<div class="card full">${empty('No vehicles','Add a vehicle manually or decode a VIN first.')}</div>`}</div>`}}catch{}
try{vehicleModal=function(v={}){modal(v.id?'Edit vehicle':'New vehicle',`<form id="vehForm" class="modal-form"><input type="hidden" name="id" value="${esc(v.id||id('vehicle'))}"><label>Name<input required name="name" value="${esc(v.name||'')}"></label><div class="form-grid"><label>Brand<input name="brand" value="${esc(v.brand||'')}"></label><label>Model<input name="model" value="${esc(v.model||'')}"></label></div><div class="form-grid"><label>Year<input type="number" name="year" value="${esc(v.year||'')}"></label><label>Plate<input name="licensePlate" value="${esc(v.licensePlate||'')}"></label></div><div class="form-grid"><label>Current km<input type="number" name="currentKm" value="${esc(v.currentKm||'')}"></label><label>Fuel<select name="fuelType">${['diesel','benzin','gas','elektro','hibrid'].map(x=>`<option ${v.fuelType===x?'selected':''}>${x}</option>`).join('')}</select></label></div><div class="form-grid"><label>Insurance expires<input type="date" name="insuranceExpires" value="${esc(day(v.insuranceExpires))}"></label><label>Registration expires<input type="date" name="registrationExpires" value="${esc(day(v.registrationExpires))}"></label></div><label>Technical expires<input type="date" name="technicalExpires" value="${esc(day(v.technicalExpires))}"></label><label>Notes<textarea name="notes">${esc(v.notes||'')}</textarea></label><label>Attachment<input type="file" name="file"></label>${attachmentsHtml(v.attachments||[])}<div class="form-actions"><button class="quiet" type="button" data-close>Cancel</button><button class="primary">Save</button></div></form>`,root=>{$('#vehForm',root).onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,x=formData(f);let atts=[...(v.attachments||[])];try{if(f.file.files[0])atts.push(await D.upload('vehicles',x.id,f.file.files[0]));await D.upsert('vehicles',{services:[],fuelHistory:[],...v,...x,attachments:atts,year:x.year?Number(x.year):undefined,currentKm:x.currentKm?Number(x.currentKm):undefined,updatedAt:iso()});await reload('vehicles');closeModal();toast('Vehicle synced');render()}catch(err){toast(err.message,true)}}})}}catch{}
try{serviceModal=function(v){modal('Add service',`<form id="svcForm" class="modal-form"><input type="hidden" name="serviceId" value="${esc(id('svc'))}"><label>Type<select name="type"><option>small</option><option>big</option><option>fuel</option><option>tires</option><option>other</option></select></label><div class="form-grid"><label>Date<input type="date" name="date" value="${day(iso())}"></label><label>KM<input type="number" name="km"></label></div><label>Cost<input type="number" step="0.01" name="cost"></label><label>Notes<textarea name="notes"></textarea></label><label>Attachment<input type="file" name="file"></label><div class="form-actions"><button class="quiet" type="button" data-close>Cancel</button><button class="primary">Add service</button></div></form>`,root=>{$('#svcForm',root).onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,x=formData(f),sid=x.serviceId||id('svc');let atts=[];try{if(f.file.files[0])atts.push(await D.upload('vehicles',`${v.id}-${sid}`,f.file.files[0]));const svc={id:sid,type:x.type,date:x.date,km:Number(x.km||0),cost:Number(x.cost||0),notes:x.notes,attachments:atts};await D.upsert('vehicles',{...v,services:[...(v.services||[]),svc],currentKm:Math.max(Number(v.currentKm||0),Number(x.km||0)),updatedAt:iso()});if(Number(x.cost||0)>0)await D.upsert('finance_transactions',{id:`vehicle-service-${v.id}-${sid}`,type:'expense',amount:Number(x.cost),currency:state.profile?.currency||'EUR',description:`Vehicle service · ${x.type}`,category:'Vehicle',date:x.date,createdAt:iso(),updatedAt:iso(),periodicity:'once',isPaid:true,sourceModule:'vehicles',sourceId:v.id});await Promise.all([reload('vehicles'),reload('finance_transactions')]);closeModal();toast('Service and linked expense synced');render()}catch(err){toast(err.message,true)}}})}}catch{}
function fuelModal(v){modal('Add fuel',`<form id="fuelForm" class="modal-form"><input type="hidden" name="fuelId" value="${esc(id('fuel'))}"><div class="form-grid"><label>Date<input type="date" name="date" value="${day(iso())}"></label><label>KM<input type="number" name="km"></label></div><div class="form-grid"><label>Liters<input type="number" min="0" step="0.01" name="liters"></label><label>Price / liter<input type="number" min="0" step="0.001" name="pricePerLiter"></label></div><label>Station<input name="station"></label><label><input type="checkbox" name="fullTank"> Full tank</label><div class="form-actions"><button class="quiet" type="button" data-close>Cancel</button><button class="primary">Add fuel</button></div></form>`,root=>{$('#fuelForm',root).onsubmit=async e=>{e.preventDefault();const f=e.currentTarget,x=formData(f),fid=x.fuelId||id('fuel'),liters=Number(x.liters||0),price=Number(x.pricePerLiter||0),total=liters*price,fuel={id:fid,date:x.date,km:Number(x.km||0),liters,pricePerLiter:price,total,station:x.station,fullTank:f.fullTank.checked};try{await D.upsert('vehicles',{...v,fuelHistory:[...(v.fuelHistory||[]),fuel],currentKm:Math.max(Number(v.currentKm||0),Number(x.km||0)),updatedAt:iso()});if(total>0)await D.upsert('finance_transactions',{id:`vehicle-fuel-${v.id}-${fid}`,type:'expense',amount:total,currency:state.profile?.currency||'EUR',description:`Vehicle fuel · ${v.name||v.licensePlate||'Vehicle'}`,category:'Fuel',date:x.date,createdAt:iso(),updatedAt:iso(),periodicity:'once',isPaid:true,sourceModule:'vehicles',sourceId:v.id});await Promise.all([reload('vehicles'),reload('finance_transactions')]);closeModal();toast('Fuel and linked expense synced');render()}catch(err){toast(err.message,true)}}})}
document.addEventListener('click',e=>{const dm=e.target.closest('[data-del-family-member]');if(dm){e.preventDefault();e.stopPropagation();return confirmRemove('family_members',dm.dataset.delFamilyMember,'family member')}const dt=e.target.closest('[data-del-family-task]');if(dt){e.preventDefault();e.stopPropagation();return confirmRemove('family_tasks',dt.dataset.delFamilyTask,'family task')}const es=e.target.closest('[data-edit-family-shop]');if(es){e.preventDefault();e.stopPropagation();return familyShopModal(collection('family_shopping').find(x=>x.id===es.dataset.editFamilyShop)||{})}const ds=e.target.closest('[data-del-family-shop]');if(ds){e.preventDefault();e.stopPropagation();return confirmRemove('family_shopping',ds.dataset.delFamilyShop,'shopping item')}const ee=e.target.closest('[data-edit-family-event]');if(ee){e.preventDefault();e.stopPropagation();return familyEventModal(collection('family_events').find(x=>x.id===ee.dataset.editFamilyEvent)||{})}const de=e.target.closest('[data-del-family-event]');if(de){e.preventDefault();e.stopPropagation();return confirmRemove('family_events',de.dataset.delFamilyEvent,'family event')}const fv=e.target.closest('[data-fuel-vehicle]');if(fv){e.preventDefault();e.stopPropagation();const v=collection('vehicles').find(x=>x.id===fv.dataset.fuelVehicle);if(v)return fuelModal(v)}},true);
})();